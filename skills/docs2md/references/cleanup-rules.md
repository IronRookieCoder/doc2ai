# 清洗规则

> **阶段1 脚本设计文档**——阶段2 AI 正常处理无需阅读此文件。仅当脚本行为异常需要排查时查阅。

脚本阶段（阶段1）对 Pandoc 输出执行的正则清洗规则。

## 1. 噪音模式清除

| 噪音模式 | 正则 | 说明 |
|---------|------|------|
| 图片引用（含维度） | `!\[.*?\]\([^)]*\)(\{[^}]*\})?` | 匹配所有图片引用，不限定路径前缀 |
| 空锚点 | `\[\]\{#[^}]+\}` | TOC 残留锚标记如 `[]{#_Toc28717 .anchor}` |
| 空链接/锚点 | `\[.*?\]\(\)` | 无效 TOC 链接 |
| 空 HTML 注释 | `<!--\s*-->` | |
| 连续空行 | `\n{3,}` → `\n\n` | 压缩为双空行 |
| 控制字符 | `[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]` | |
| 行尾反斜杠 | `\\\s*$`（多行模式） | Markdown 软换行标记 |
| 全角空格缩进 | `^[　]+`（多行模式） | 中文全角空格段首缩进，Markdown 中无语义 |
| HTML 原始块 | `^```\{=html\}\n[\s\S]*?\n```$`（多行模式） | Pandoc 输出的页码域代码等 RawBlock 残留 |

## 2. 实体与转义处理

- XML 残留实体（`&amp;`、`&lt;`、`&gt;`、`&apos;`、`&quot;` 及数字字符引用）用 `html.unescape()` 处理
- Pandoc 主动生成的 HTML 格式实体（如表格单元格中的 `&nbsp;`）不做 unescape，保留原始实体形式以保证可读性
- 转义字符**仅按模式精确清理，禁止全局去转义**

## 3. 标题空白规范化

始终确保 ATX 标题行（`# ` 开头）前后各有恰好一个空行。若已有空行则不重复添加，若紧邻其他内容行则补齐空行。

## 4. 列表与复杂表格

- Pandoc 对 docx 列表和简单表格的转换通常可靠，无需额外规则清洗
- Pandoc 在表格单元格中使用 `<br>` 标签表示行内换行，属于合法 GFM 格式，不做清除
- 合并单元格的复杂表格，Pandoc 可能输出不规则的管线表格或丢失合并信息——脚本阶段不做修正，交由 AI 阶段在语义层面判断和修复
- 若 Pandoc 在正文区域输出旧式格式表格（`--- ---` 分隔符而非 GFM 管线格式），脚本阶段不做格式转换，交由 AI 后处理阶段判断和修复
